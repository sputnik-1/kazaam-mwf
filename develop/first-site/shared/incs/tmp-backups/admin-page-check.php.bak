<?php

// first-site website administrator's access checker.
// file-id: /first-site/shared/incs/admin-page-check.php

// make sure the user is authorised to access this page

// turn off error reporting for all admin pages
// this removes all error messages from admin pages
// such as undefined indexes
// uncomment the line below to turn error reporting OFF
// error_reporting(0);

// error reporting is also set in the global site-config.php file
// for ALL pages - so that setting may affect this option as well.


// uncomment the block below to activate admin page checks
// for ALL admin pages globally

/*
// get the site admin's username and pasword
require "$SHARED_INCS/admin-details.php";

$logon_username = $_SESSION['logon_username'];
$logon_password = $_SESSION['logon_password'];

// SiteAdmin class
require "$SHARED_CLASSES/class.SiteAdmin.php";
$adm = &new SiteAdmin($sa_username, $sa_password,
                      $logon_username, $logon_password);

$adm->page_check();

*/

?>
