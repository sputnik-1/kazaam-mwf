<?php

 // first-site website configuration.
 // file-id: /first-site/shared/incs/site-config.php

 // This file sets config values for a particular website,
 // and is included by default at the top of each page that needs it.

 // This config file can be encoded by ioncube encoder
 // hiding the path layouts.

/* ------------------------------------------- */
 // module-specific shared files

 // path to module specific config files
 $MODULE_CONFIG = "../local/incs";

 // path to this module's local file content 
 $MODULE_PATH = "../local/incs/mod";

 // path to this module's local images
 $LOCAL_IMAGES = "../local/images";

 // path to this module's local include files
 $LOCAL_INCS = "../local/incs";

 // path to this module's local class files
 $LOCAL_CLASSES = "../local/classes";

/* ------------------------------------------- */
 // site-wide shared file paths

 // path to site-wide shared include files
 // and other site-wide includes
 $CONFIG_PATH = "../../shared/incs";

 // path to site-wide shared include files
 // and other site-wide includes
 $SHARED_INCS = "../../shared/incs";

/*
 // using an array - works
 $KAZ['shared_incs'] = "../../shared/incs";  

 // using constants - works
 define("CONSTANT_NAME", "../../shared/incs");
*/

 // path to site-wide shared php classes
 $SHARED_CLASSES = "../../shared/classes";

 // path to site-wide shared meta-menus
 $SHARED_META_MENUS = "../../shared/meta-menus";

 // path to site-wide shared images
 $SHARED_IMAGES = "../../shared/images";

 // path to site-wide style themes 
 $SITE_THEMES = "../../shared/themes/default";

/* ------------------------------------------- */

 // Allow the website admin to alter the website configuration globally.
 // The included user.conf.php config file will not be encoded.
 // All files ending with .conf.php are not meant to be encoded.
 require "$SHARED_INCS/user.conf.php";

?>
